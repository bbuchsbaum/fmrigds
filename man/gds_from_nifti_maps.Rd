% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nifti-utils.R
\name{gds_from_nifti_maps}
\alias{gds_from_nifti_maps}
\title{Construct a GDS from NIfTI maps discovered on disk}
\usage{
gds_from_nifti_maps(maps, mask = NULL, ...)
}
\arguments{
\item{maps}{A data.frame (or tibble) with at least a \code{file} column of
NIfTI paths. Optional \code{subject} and \code{contrast} columns can be used to
relabel the realised GDS.}

\item{mask}{Optional mask passed through to \code{\link[=gds]{gds()}} when constructing the
NIfTI plan. Can be a NIfTI file path or a \code{neuroim2::NeuroVol}, matching
the NIfTI adapter contract.}

\item{...}{Additional arguments forwarded to \code{\link[=gds]{gds()}} when building the
NIfTI-backed plan.}
}
\value{
A realised GDS object.
}
\description{
This helper builds on the NIfTI adapter to materialise a realised GDS from
a set of NIfTI map files, typically obtained via \code{\link[=find_maps]{find_maps()}}. The heavy
lifting (reading NIfTI, spatial metadata, masking) is delegated to the
existing NIfTI adapter and the \code{neuroim2} package.
}
\details{
Subject and contrast labels can be injected post-hoc using the \code{subject} and
\code{contrast} columns when available in \code{maps}. This avoids hard-coding any
filename convention while still allowing callers to impose semantics via
hooks.
}
