% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adapter-nifti.R
\name{register_nifti_adapter}
\alias{register_nifti_adapter}
\title{Register NIfTI Adapter}
\usage{
register_nifti_adapter()
}
\value{
NULL (invisibly)
}
\description{
Registers the NIfTI adapter for reading neuroimaging data in NIfTI format.
Uses the neuroim2 package for reading NIfTI files.
}
\details{
The NIfTI adapter supports:
\itemize{
\item 3D, 4D, and 5D+ NIfTI files (.nii, .nii.gz)
\itemize{
\item For 5D+, non-spatial axes (dims 4+) are flattened into the GDS contrast axis
}
\item Optional mask specification (separate NIfTI file with 1=included, 0=excluded)
\item Automatic spatial information extraction (affine transformation)
\item BIDS-compatible file naming
}

Source specification options:
\itemize{
\item Character vector or directory: pass one or more \code{.nii}/\code{.nii.gz} paths, or a
directory. The adapter will attempt to classify files into beta vs se sets by
filename patterns: \code{beta|cope|effect} for beta, and \verb{(se|stderr|sterr|sigma|std(err)?)}
occurring with separators like \verb{_}, \code{.}, or \code{-}.
\item Named list: pass \verb{list(beta = <paths/dir>, se = <paths/dir>)} to explicitly
provide the sets. See \code{\link[=nifti_source]{nifti_source()}}.
}

Beta-only NIfTI sources are accepted for raw map workflows. When a realised
GDS is materialised from beta maps without standard-error images, the adapter
creates a synthetic \code{var} assay filled with 1 and emits a warning. That
variance is only a placeholder; use unweighted reducers such as
\code{method = "ols:voxelwise"} for group analyses of subject-level stat maps
rather than fixed/random-effects meta-analysis.

To provide a mask, pass it to gds() via the mask parameter:
\code{gds(source, mask = "path/to/mask.nii")}
}
\keyword{internal}
