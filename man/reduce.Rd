% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verb-reduce.R
\name{reduce}
\alias{reduce}
\title{Reduce across subjects (meta-analysis)}
\usage{
reduce(
  x,
  method = c("fixed", "random", "stouffer", "fisher"),
  weights = c("1/var", "n_eff", "equal", "custom"),
  by = "contrast",
  options = list(),
  formula = NULL,
  data = NULL
)
}
\arguments{
\item{x}{A \code{\link{gds_plan}}, \code{\link{gds_source}}, or realised \code{\link{gds}}}

\item{method}{Reduction method. Built-ins include \code{"fixed"}, \code{"random"},
\code{"stouffer"}, and \code{"fisher"}. Registry-backed reducers include
meta-analytic regression reducers such as \code{"meta:fe_reg"} and the
restricted repeated-measures Gaussian LMM reducers \code{"lmm:ri"} and
\code{"lmm:ri_slope1"}, plus permutation reducers \code{"perm:onesample"} and
\code{"perm:twosample"}.}

\item{weights}{Weighting scheme (\code{"1/var"}, \code{"n_eff"}, \code{"equal"}, \code{"custom"})}

\item{by}{Grouping variable (e.g., \code{"contrast"})}

\item{options}{Additional reducer options. Common examples include
\code{tau2 = NULL}, \code{custom_weights}, or for restricted LMM reducers
\code{list(fit = "REML", theta_mode = "pooled")} plus
\code{list(slope = "time", covariance = "diag")} for \code{"lmm:ri_slope1"}.

Note: Current grouping is per-contrast. Values other than "contrast" are
not interpreted yet; future versions may extend grouping to other
factors in \code{row_data}/\code{col_data}.

Restricted repeated-measures LMM contract:
\itemize{
\item \code{"lmm:ri"} fits a Gaussian random-intercept model.
\item \code{"lmm:ri_slope1"} fits a Gaussian random intercept plus one within-subject slope.
\item Repeated-measure metadata must live on the contrast axis via \code{\link[=with_contrast_data]{with_contrast_data()}}
or tabular ingestion with \code{gds(..., contrast_data_cols = ...)}.
\item \code{options$theta_mode} can be \code{"pooled"} or \code{"voxelwise"}.
\item The supported family is intentionally narrow: one grouping factor only,
Gaussian responses only, and no general random-effects formula grammar.
}

Permutation reducer contract:
\itemize{
\item \code{"perm:onesample"} performs an unweighted one-sample sign-flip t test
for each sample and contrast.
\item \code{"perm:twosample"} performs an unweighted two-sample label-permutation
t test. It can infer the tested two-level group column from \code{formula}
and \code{col_data}, or use \code{options$group} directly.
\item Common options include \code{n_perm}, \code{seed}, \code{alternative}, and for
\code{"perm:twosample"} \code{variance = "welch"} or \code{"pooled"}.
\item Outputs include \code{t_g}, parametric \code{p_g}, permutation \code{p_perm}, and
max-|t| family-wise \code{p_fwer}.
}}

\item{formula}{Optional model formula. For meta-regression reducers the design
is built from subject-level \code{col_data}. For \code{"lmm:ri"} and
\code{"lmm:ri_slope1"}, the formula specifies fixed effects at the
subject-by-repeat level and must not contain \code{lmer}-style random-effects
syntax.}

\item{data}{Optional data frame for building \code{X} when \code{x} is not a realised GDS.}
}
\value{
Updated plan
}
\description{
Reduce across subjects (meta-analysis)
}
\details{
\code{reduce()} always works on a lazy plan internally, so \code{x} is whichever stage
of the fmrigds workflow you already have:
\itemize{
\item Start from files or another external source with \code{\link[=gds]{gds()}}. That returns a
\code{\link{gds_plan}} you can pipe directly into \code{reduce()}.
\item Start from an in-memory result with a realised \code{\link{gds}} returned by
\code{\link[=compute]{compute()}} or created directly with \code{\link[=new_gds]{new_gds()}}. \code{reduce()} will convert
it with \code{\link[=as_plan]{as_plan()}} for you.
\item You can also pass a low-level \code{\link{gds_source}} created by \code{\link[=gds_source]{gds_source()}}, but
most users do not need this because \code{\link[=gds]{gds()}} creates the source binding
automatically.
}

If you want that conversion to be explicit, use \code{\link[=as_plan]{as_plan()}} or its alias
\code{\link[=plan]{plan()}} before calling \code{reduce()}.

For worked examples, see \code{vignette("fmrigds")} for the basic source -> plan
-> compute workflow and \code{vignette("as-plan-and-spatial-fdr")} for chaining
verbs on realised GDS objects.
}
\section{Output assays}{

Reducers collapse the subject axis and write group-level assays (named on the
realised GDS, retrievable with \code{\link[=assays]{assays()}}/\code{\link[=assay]{assay()}}):
\itemize{
\item \code{"ols:voxelwise"}: per-term \verb{coef:<term>}, \verb{se_coef:<term>},
\verb{t_coef:<term>}, \verb{p_coef:<term>}, plus \code{sigma2}, \code{df_res}, \code{n_obs}
(see \link{reducer-ols-voxelwise}).
\item \code{"fixed"}/\code{"meta:fe"} and \code{"random"}/\code{"meta:re"}: \code{beta_g}, \code{var_g},
\code{se_g}, \code{z_g}, \code{p_g}, \code{Q}, \code{I2}, \code{n_eff} (random-effects also adds
\code{tau2}). \code{n_eff} is the number of finite subject-level inputs used for each sample and contrast.
\item \code{"meta:fe_reg"}/\code{"meta:re_reg"}: per-term \verb{coef:<term>}/\verb{se_coef:<term>}
plus \code{Q}, \code{df_res} (random-effects also adds \code{tau2}).
\item \code{"stouffer"}/\code{"combine:stouffer"}: \code{z_g}, \code{p_g}.
\item \code{"fisher"}/\code{"combine:fisher"} and \code{"combine:lancaster"}: \code{chi2}, \code{df},
\code{p_g}.
\item \code{"perm:onesample"}/\code{"perm:twosample"}: \code{beta_g}, \code{se_g}, \code{t_g}, \code{df},
\code{p_g}, \code{p_perm}, \code{p_fwer}.
}
}

\seealso{
\code{\link[=gds]{gds()}}, \code{\link[=compute]{compute()}}, \code{\link[=as_plan]{as_plan()}}, \code{\link[=plan]{plan()}}, \code{\link[=new_gds]{new_gds()}}, \code{\link[=gds_source]{gds_source()}}
}
