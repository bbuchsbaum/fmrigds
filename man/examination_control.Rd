% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/examination-control.R
\name{examination_control}
\alias{examination_control}
\title{Configure group examination}
\usage{
examination_control(
  block_size = 1024L,
  geometry = list(rank = 32L, oversample = 8L, cap = 8, balance_contrasts = TRUE,
    stability_replicates = 2L),
  review = list(surprise = list(energy_threshold = 2.5, tail_threshold = 0.01,
    residual_threshold = 3), influence = list(energy_threshold = 1, max_abs_threshold =
    2), quality = list(coverage_fraction = list(direction = "low", threshold = 0.8)),
    min_stability = 0.7),
  exact_refit_n = 5L,
  retain_n = 5L,
  staging = list(tempdir = NULL),
  tolerance = list(rank = sqrt(.Machine$double.eps), leverage = 1e-08, degeneracy =
    1e-12)
)
}
\arguments{
\item{block_size}{Positive number of samples read per scan block.}

\item{geometry}{Named list controlling the deterministic low-rank residual
representation. Supported entries are \code{rank}, \code{oversample}, \code{cap},
\code{balance_contrasts}, and \code{stability_replicates}.}

\item{review}{Named list controlling review status. Supported entries are
\code{surprise}, \code{influence}, \code{quality}, and \code{min_stability}.}

\item{exact_refit_n}{Number of selected random-effects cases eligible for an
exact heterogeneity refit.}

\item{retain_n}{Number of highest-priority subjects retained for localization
when no explicit \code{retain} set is supplied.}

\item{staging}{Named list with optional \code{tempdir}. Staging stores are always
removed on success or failure.}

\item{tolerance}{Named list of numerical tolerances: \code{rank}, \code{leverage}, and
\code{degeneracy}.}
}
\value{
An object of class \code{gds_examination_control}.
}
\description{
\code{examination_control()} validates the bounded execution, residual-geometry,
review, staging, and numerical settings used by \code{\link[=examine_group]{examine_group()}}. Keeping
these settings in a typed object makes defaults visible and reproducible.
}
