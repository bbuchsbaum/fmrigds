% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scalar-map-workflow.R
\name{write_nifti_assays}
\alias{write_nifti_assays}
\title{Write image-like GDS assays as NIfTI files}
\usage{
write_nifti_assays(
  g,
  out_dir,
  prefix = NULL,
  assays = NULL,
  subjects = NULL,
  contrasts = NULL,
  sanitize = TRUE,
  overwrite = FALSE
)
}
\arguments{
\item{g}{A realised \code{\link{gds}} with voxel space.}

\item{out_dir}{Output directory.}

\item{prefix}{Optional filename prefix.}

\item{assays}{Character vector of assays to write. Defaults to all assays.}

\item{subjects}{Optional subject labels to write. Defaults to all subjects.}

\item{contrasts}{Optional contrast labels to write. Defaults to all contrasts.}

\item{sanitize}{Logical; if \code{TRUE}, make filenames filesystem-friendly.}

\item{overwrite}{Logical; if \code{FALSE}, existing files are skipped.}
}
\value{
A data.frame manifest with \code{assay}, \code{subject}, \code{contrast}, \code{path},
\code{written}, and \code{skipped_reason} columns.
}
\description{
Writes each selected assay/subject/contrast image in a voxel-space GDS to a
separate NIfTI file and returns a manifest describing written and skipped
outputs. This is intended for model outputs such as \verb{coef:*}, \verb{t_coef:*},
and \verb{p_coef:*} assays.
}
\examples{
\dontrun{
fit <- group_ols(g, ~ group) |> compute()
manifest <- write_nifti_assays(fit, "group_maps", prefix = "auc")
}
}
