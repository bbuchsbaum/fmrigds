% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frame-gds-projection.R
\name{as_gds.fmri_frame}
\alias{as_gds.fmri_frame}
\title{Project a rectangular frame to the legacy GDS cube}
\usage{
\method{as_gds}{fmri_frame}(
  x,
  subject = "subject_id",
  contrast = "contrast_id",
  subjects = NULL,
  contrasts = NULL,
  assay_names = names(fmridataset::assays(x)),
  assay_map = NULL,
  memory_budget = 2 * 1024^3,
  metadata = list(),
  ...
)
}
\arguments{
\item{x}{An \code{fmri_frame} or synchronized frame view.}

\item{subject, contrast}{Observation columns defining the rectangular axes.
Either may be \code{NULL} only for an explicit singleton axis.}

\item{subjects, contrasts}{Optional explicit axis order or singleton label.
A \code{NULL} subject column defaults to a singleton \code{"meta"} axis; a \code{NULL}
contrast column defaults to \code{"contrast1"}.}

\item{assay_names}{Assays to realize into the cube.}

\item{assay_map}{Named character vector mapping frame assay names to GDS
assay names. By default \code{variance} becomes \code{var} and \code{std_error} becomes
\code{se}.}

\item{memory_budget}{Maximum total bytes realized across projected assays.}

\item{metadata}{Additional GDS metadata.}

\item{...}{Reserved for future use.}
}
\value{
A realized legacy \code{gds} compatibility projection.
}
\description{
This compatibility projection is deliberately strict. The selected
observation annotations must define one complete, unique Cartesian product
of subject and contrast keys. Ragged frames remain frames and are rejected
rather than padded with implicit slabs.
}
