% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posthoc-registry.R
\name{register_posthoc}
\alias{register_posthoc}
\title{Register a post-hoc method}
\usage{
register_posthoc(name, fun, requires, provides, overwrite = TRUE)
}
\arguments{
\item{name}{String identifier, e.g., "fdr:bh"}

\item{fun}{Function(arrays, opts) -> list of arrays (delta or full arrays)}

\item{requires}{Character vector of required input assays, e.g., c("p")}

\item{provides}{Character vector of output assay names, e.g., c("q")}

\item{overwrite}{Logical; if FALSE and a handler with \code{name} exists, error}
}
\description{
Register or replace a post-hoc handler identified by \code{name}.
}
\details{
The handler \code{fun} is called as \code{fun(arrays, opts)} where \code{arrays} is a
named list of assays shaped \verb{sample x subject x contrast}. It should return
either (a) a named list of new/updated assays to merge (e.g., \code{list(q = ...)}),
or (b) the full \code{arrays} list with modifications. Implementations must
preserve the input array dimensions.
}
