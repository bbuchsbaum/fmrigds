% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export-neuroim.R
\name{gds_from_neurovol_nested}
\alias{gds_from_neurovol_nested}
\title{Create GDS from nested structure of NeuroVol objects}
\usage{
gds_from_neurovol_nested(
  beta,
  var = NULL,
  se = NULL,
  contrasts = NULL,
  mask = NULL,
  col_data = NULL,
  metadata = list(),
  ...
)
}
\arguments{
\item{beta}{A nested list structure for beta/effect estimates. Can be:
\itemize{
\item Nested list: \code{list(subject1 = list(con1 = vol, con2 = vol), ...)}
\item List of 4D NeuroVec: \code{list(subject1 = vec4d, subject2 = vec4d, ...)}
where each NeuroVec has contrasts along the 4th dimension
}}

\item{var}{Optional matching structure for variance estimates.}

\item{se}{Optional matching structure for standard errors (alternative to var).}

\item{contrasts}{Character vector of contrast names. Required when using
NeuroVec inputs; inferred from inner list names otherwise.}

\item{mask}{Optional mask. See \code{as_gds.NeuroVol} for options.}

\item{col_data}{Optional data frame of subject-level covariates.}

\item{metadata}{Optional list of additional metadata to attach.}

\item{...}{Additional arguments passed to \code{\link[=new_gds]{new_gds()}}.}
}
\value{
A \code{gds} object with dimensions \verb{samples x subjects x contrasts}.
}
\description{
The most flexible import function for creating GDS objects from neuroim2
data. Supports multiple subjects and multiple contrasts, with proper
variance/SE handling.
}
\examples{
\dontrun{
# Example 1: Nested list structure (subjects x contrasts)
beta <- list(
  `sub-01` = list(
    faces = neuroim2::read_vol("sub-01_faces_beta.nii.gz"),
    places = neuroim2::read_vol("sub-01_places_beta.nii.gz")
  ),
  `sub-02` = list(
    faces = neuroim2::read_vol("sub-02_faces_beta.nii.gz"),
    places = neuroim2::read_vol("sub-02_places_beta.nii.gz")
  )
)
var <- list(
  `sub-01` = list(
    faces = neuroim2::read_vol("sub-01_faces_var.nii.gz"),
    places = neuroim2::read_vol("sub-01_places_var.nii.gz")
  ),
  `sub-02` = list(
    faces = neuroim2::read_vol("sub-02_faces_var.nii.gz"),
    places = neuroim2::read_vol("sub-02_places_var.nii.gz")
  )
)

gds_obj <- gds_from_neurovol_nested(beta, var = var)

# Example 2: List of 4D NeuroVecs (each subject has multi-contrast 4D file)
beta_vecs <- list(
  `sub-01` = neuroim2::read_vec("sub-01_allcons_beta.nii.gz"),
  `sub-02` = neuroim2::read_vec("sub-02_allcons_beta.nii.gz")
)
gds_obj <- gds_from_neurovol_nested(beta_vecs,
                                     contrasts = c("faces", "places", "objects"))
}
}
