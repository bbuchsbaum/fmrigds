% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/local-displacement-rescue.R
\name{local_displacement_rescue}
\alias{local_displacement_rescue}
\title{Experimental local displacement rescue at one voxel}
\usage{
local_displacement_rescue(
  split_a,
  split_b,
  center,
  contrast = NULL,
  patch_radius_mm = 12,
  shift_radius_mm = 4,
  tau_grid_mm = NULL,
  folds = 5L,
  min_subjects = 12L,
  iterations = 20L,
  ridge = 1e-04,
  tolerance = 1e-05,
  n_resamples = 0L,
  seed = 1L
)
}
\arguments{
\item{split_a, split_b}{Realised, independent subject-level split maps with
matching voxel space, subjects, contrasts, \code{beta}, and genuine \code{var} or
\code{se} assays.}

\item{center}{One-based full-volume voxel index anchoring the positive core.
For packed inputs this is still the index in the full voxel grid.}

\item{contrast}{Optional contrast name or index. Required for multi-contrast
inputs.}

\item{patch_radius_mm}{Positive physical radius of the fitted patch. It must
be at least twice \code{shift_radius_mm}.}

\item{shift_radius_mm}{Positive maximum translation radius in millimetres.}

\item{tau_grid_mm}{Positive candidate displacement scales. Selection occurs
only in training folds.}

\item{folds}{Number of deterministic subject cross-fitting folds.}

\item{min_subjects}{Minimum number of paired subjects.}

\item{iterations}{Maximum template EM iterations per model and fold.}

\item{ridge}{Positive template normal-equation ridge.}

\item{tolerance}{Positive relative convergence tolerance.}

\item{n_resamples}{Number of resamples for ROI calibration. Zero skips
calibration and returns \code{NA} for \code{ldr_p}. Use at least 199 for exploratory
work and more for small tail probabilities.}

\item{seed}{Integer seed used only inside calibration. The caller's random
number state is restored.}
}
\value{
A group-level \code{\link{gds}} with three assays, finite only at \code{center}:
\code{ldr_p}, \code{mni_loss}, and \code{shift_rms_mm}. \code{ldr_p} is \code{NA} when calibration
is skipped. Supporting scores and model quantities are stored under
\code{metadata(result)$ldr}.
}
\description{
Fits an exact, paired-split reference model for a compact signed feature at
one prespecified voxel. The function compares a positive aligned feature, a
positive randomly displaced feature, and an aligned signed-amplitude
heterogeneity model. Subject shifts are integrated rather than selected in
held-out data, and every reported score is evaluated by subject
cross-fitting.
}
\section{Scientific contract}{

The activation component compares the displaced positive-feature model with
no local feature. The displacement component is the smaller held-out improvement
over (1) the aligned positive-feature model and (2) the aligned signed-
amplitude heterogeneity model. Requiring both comparisons prevents stable
polarity mixtures from being attributed to spatial displacement.
\code{mni_loss} is the change in the ordinary one-sample t statistic after a
cross-fitted posterior correction that sets displacement to zero while
retaining each subject's observed center value. \code{shift_rms_mm} is the mean
RMS scale of the shift prior selected inside the training folds.
}

\section{Experimental}{

This is the exact ROI reference implementation, not whole-brain inference.
When \code{n_resamples > 0}, \code{ldr_p} is an ROI-level intersection-union p-value:
activation is calibrated by paired whole-subject sign flips, while
displacement and MNI loss are calibrated by parametric bootstraps from both
aligned competitor models. The complete cross-fitting and scale selection
procedure is rerun in every resample. No spatial search, FWER correction, or
LDR flag is returned. The current noise likelihood uses the supplied diagonal
\code{var}/\code{se} precision after removing a constant and available linear spatial
trends. Spatial-covariance estimation, the tangent screen, and whole-brain
maximum-statistic inference remain separate future stages.
}
