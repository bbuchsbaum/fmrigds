% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/experimental-cancellation.R
\name{experimental_cancellation}
\alias{experimental_cancellation}
\title{Experimental sign-cancellation and spatial-dispersion maps}
\usage{
experimental_cancellation(
  x,
  delta,
  equivalence = delta,
  prevalence = 0.2,
  support_probability = 0.95,
  shift_radius = 1L,
  patch_radius = 2L,
  regions = NULL,
  min_subjects = 8L,
  translation_penalty = 0.01,
  mass_retention = c(0.75, 1.25),
  min_correlation_gain = 0.05,
  min_aligned_correlation = 0.5
)
}
\arguments{
\item{x}{A realised subject-level \code{\link{gds}} with aligned \code{beta} and genuine
\code{var} or \code{se} assays in voxel space. One map per subject is sufficient.}

\item{delta}{Positive smallest meaningful subject-level effect, in beta units.}

\item{equivalence}{Positive practical-equivalence bound for the population
mean. Defaults to \code{delta}.}

\item{prevalence}{Required positive and negative population prevalence for
\code{cancellation_probability}; also the minimum fraction of subjects supporting
an automatic local region. Must be strictly between 0 and 0.5.}

\item{support_probability}{Nominal subject-level measurement-model
probability of an effect outside \verb{[-delta, delta]} used for automatic
region support. The per-location threshold is Bonferroni-adjusted over the
searched offsets. Defaults to 0.95.}

\item{shift_radius}{Maximum integer translation in voxels along each axis.}

\item{patch_radius}{Integer dilation around each local region used to estimate
subject-to-template pattern correlation.}

\item{regions}{Optional region labels with one value per active sample or full
voxel. Zero and \code{NA} are excluded. When omitted, automatic local regions are
formed from meaningful-effect support.}

\item{min_subjects}{Minimum number of valid subjects for either diagnostic.}

\item{translation_penalty}{Nonnegative penalty per squared voxel shifted.}

\item{mass_retention}{Length-two accepted interval for retained meaningful
local signal mass after translation.}

\item{min_correlation_gain}{Minimum correlation improvement required to accept
a nonzero subject displacement.}

\item{min_aligned_correlation}{Minimum signed correlation with the
leave-one-subject-out template after alignment. This prevents an inverted
pattern from appearing rescued merely because it was shifted away.}
}
\value{
A group-level \code{\link{gds}} with exactly two assays:
\code{cancellation_probability} and \code{shift_rescue_fraction}. Spatial rescue is
\code{NA} where no supported local region or too few valid subject patterns are
available; an evaluated region with no accepted displacement returns zero.
}
\description{
Estimates two complementary diagnostics from one set of subject-level maps.
\code{cancellation_probability} is the plug-in posterior probability, under a
normal random-effects model, that meaningful positive and negative population
prevalences both exceed \code{prevalence} while the population mean lies inside
\verb{[-equivalence, equivalence]}. \code{shift_rescue_fraction} asks a different,
region-level question: what fraction of each subject's mismatch to an
independently formed leave-one-subject-out template is removed by a small
local translation? The region summary is projected back to its voxels.
}
\section{Experimental}{

This interface and its statistical model may change. Neither output is
corrected whole-brain inference. Spatial rescue describes between-subject
localisation variability; it does not test movement within a subject. A
subject is excluded from its own template, but its shift and rescue are
estimated from the same subject-level map. Treat the result as a descriptive
diagnostic unless it is confirmed in independent data.
}

\section{Automatic regions}{

When \code{regions} is omitted, a subject supports a voxel when the
measurement-model probability of an effect outside \verb{[-delta, delta]}
somewhere within \code{shift_radius} reaches the search-adjusted threshold implied
by \code{support_probability}. Voxels enter the spatial diagnostic when at least
\code{prevalence} of subjects support them; nearby support is joined into
six-connected local regions. This rule is independent of
\code{cancellation_probability}, so a same-signed activation that varies in
location can be evaluated.
}
