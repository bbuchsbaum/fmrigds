% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/experimental-cancellation.R
\name{experimental_cancellation}
\alias{experimental_cancellation}
\title{Experimental cancellation and fast spatial-rescue maps}
\usage{
experimental_cancellation(
  split_a,
  split_b,
  delta,
  equivalence = delta,
  prevalence = 0.2,
  shift_radius = 1L,
  patch_radius = 2L,
  candidate_threshold = 0.5,
  regions = NULL,
  min_subjects = 8L,
  translation_penalty = 0.01,
  mass_retention = c(0.75, 1.25),
  min_correlation_gain = 0,
  min_aligned_correlation = 0.5
)
}
\arguments{
\item{split_a, split_b}{Realised subject-level \code{\link{gds}} objects from independent
runs or balanced data splits. Both require aligned \code{beta} and genuine \code{var}
or \code{se} assays, identical voxel space, subjects, and contrasts.}

\item{delta}{Positive smallest meaningful subject-level effect, in beta units.}

\item{equivalence}{Positive practical-equivalence bound for the population
mean. Defaults to \code{delta}.}

\item{prevalence}{Required positive and negative population prevalence;
strictly between 0 and 0.5.}

\item{shift_radius}{Maximum integer translation in voxels along each axis.}

\item{patch_radius}{Integer dilation around each candidate region used to
estimate signed patch correlation.}

\item{candidate_threshold}{Minimum cancellation probability at which spatial
rescue is evaluated.}

\item{regions}{Optional region labels with one value per active sample or full
voxel. Zero and \code{NA} are excluded. When omitted, six-connected candidate
components are used.}

\item{min_subjects}{Minimum number of valid subjects at a voxel.}

\item{translation_penalty}{Nonnegative penalty per squared voxel shifted.}

\item{mass_retention}{Length-two accepted interval for held-out unsigned patch
mass after translation.}

\item{min_correlation_gain}{Required held-out signed patch-correlation gain.}

\item{min_aligned_correlation}{Minimum mean held-out signed correlation after
alignment. This guards against apparent rescue caused by shifting an
inverted pattern away from a strongly negative match.}
}
\value{
A group-level \code{\link{gds}} with exactly two assays:
\code{cancellation_probability} and \code{shift_rescue_fraction}.
}
\description{
Estimates two deliberately narrow voxelwise diagnostics from independent
subject-level split maps. \code{cancellation_probability} is the plug-in posterior
probability, under a normal random-effects model, that meaningful positive
and negative population prevalences both exceed \code{prevalence} while the mean
lies inside \verb{[-equivalence, equivalence]}. \code{shift_rescue_fraction} estimates
the fraction of thresholded cancellation removed by a small local translation
learned on one split and evaluated on the other, averaged over both split
directions.
}
\section{Experimental}{

This interface and its statistical model may change. Spatial rescue is a
diagnostic, not corrected whole-brain inference. It is returned only where
cancellation exceeds \code{candidate_threshold} and both held-out folds preserve
local unsigned mass and improve signed patch correlation.
}
