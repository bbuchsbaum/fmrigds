% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reducer-registry.R
\name{register_reducer}
\alias{register_reducer}
\title{Register a reducer kernel}
\usage{
register_reducer(
  name,
  fun,
  requires,
  provides,
  options_schema = list(),
  input_shape = c("contrastwise", "joint_contrast"),
  model_contract = NULL,
  diagnostics = NULL,
  frame_fun = NULL
)
}
\arguments{
\item{name}{String identifier, e.g., "meta:fe"}

\item{fun}{Function(beta, var, X, z, p, df, df1, df2, opts) -> named list}

\item{requires}{Character vector of required inputs, e.g., c("beta","var")}

\item{provides}{Character vector of outputs to be written}

\item{options_schema}{Optional schema for options}

\item{input_shape}{Reducer execution mode: \code{"contrastwise"} (default) or
\code{"joint_contrast"} for reducers that consume the full contrast axis jointly}

\item{model_contract}{Optional declarative description of the reducer's
design, weighting, missingness, variance, estimand, and deletion semantics.
Custom reducers may omit this, in which case model-conditioned examination
diagnostics are reported as unavailable.}

\item{diagnostics}{Optional diagnostic implementation declaration. This is a
list containing \code{fun}, \code{capabilities}, and \code{modes}; \code{fun} may be \code{NULL}
while a capability is declared but not yet implemented.}

\item{frame_fun}{Optional feature-block kernel for \code{fmri_frame} execution.
It must accept \code{arrays}, \code{design}, and \code{options}, and return aligned result
assays, result-observation metadata, and per-feature diagnostics.}
}
\value{
Invisibly, the registered reducer \code{name}.
}
\description{
Register a reducer kernel
}
