% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scalar-map-workflow.R
\name{group_ols}
\alias{group_ols}
\alias{one_sample}
\alias{two_sample}
\title{Voxelwise OLS helpers for scalar-map group analyses}
\usage{
group_ols(x, formula = ~1, col_data = NULL, options = list(), ...)

one_sample(x, subset = NULL, col_data = NULL, options = list(), ...)

two_sample(
  x,
  group = "group",
  baseline = NULL,
  level = NULL,
  col_data = NULL,
  options = list(),
  ...
)
}
\arguments{
\item{x}{A realised \code{\link[=gds]{gds}} or \code{\link[=gds_plan]{gds_plan}}.}

\item{formula}{One-sided model formula for \code{group_ols()}.}

\item{col_data}{Optional subject-level covariates to attach before fitting.}

\item{options}{Reducer options passed to \code{\link[=reduce]{reduce()}}.}

\item{...}{Additional arguments passed to \code{\link[=reduce]{reduce()}}.}

\item{subset}{Optional expression evaluated in \code{col_data(x)} to select
subjects before fitting the one-sample model.}

\item{group}{Name of the grouping column in \code{col_data(x)}.}

\item{baseline}{Optional reference level for the grouping factor.}

\item{level}{Optional non-baseline level to compare against \code{baseline}. When
omitted, all non-baseline levels are retained in the model.}
}
\value{
A \code{\link[=gds_plan]{gds_plan}} with an OLS reduce node.
}
\description{
These helpers wrap \code{reduce(method = "ols:voxelwise")} for subject-level map
workflows. They return a lazy GDS plan; call \code{\link[=compute]{compute()}} to
materialise the fitted maps.
}
\examples{
\dontrun{
fit <- group_ols(g, ~ group) |> compute()
one <- one_sample(g) |> compute()
two <- two_sample(g, group = "diagnosis", baseline = "control") |> compute()
}
}
