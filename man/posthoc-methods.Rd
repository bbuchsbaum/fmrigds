% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posthoc-registry.R
\name{posthoc-methods}
\alias{posthoc-methods}
\title{Built-in post-hoc correction methods and their output assays}
\value{
This is a documentation page (no return value).
}
\description{
Methods are selected by name in \code{\link[=posthoc]{posthoc()}} and add their adjusted-p assays
to the realised GDS. The FDR methods adjust the object's p-value assay(s):
a genuine bare \code{p} (from meta/combine reducers or a tabular \code{p} column) yields
a single \code{q}; a per-term \verb{p_coef:<term>} family (from regression/LMM reducers
such as \code{ols:voxelwise}) yields one \verb{q_coef:<term>} per term. Pass
\code{options = list(source = "p_coef:<term>")} to adjust one specific term.
}
\section{Methods and outputs}{

\itemize{
\item \code{"fdr:bh"}, \code{"fdr:by"}: add \code{q} (or per-term \verb{q_coef:<term>}),
Benjamini-Hochberg / Benjamini-Yekutieli adjusted p-values, computed
column-wise per subject x contrast.
\item \code{"fdr:spatial"}: adds \code{q} (or per-term \verb{q_coef:<term>}), group-wise spatial
FDR. Requires a discrete spatial grouping (one parcel/cluster id per
sample) via \code{options$group} or a recognised \code{row_data} column
(\code{feature_group}, \code{spatial_group}, \code{group}, \code{parcel}, \code{parcel_id}).
\item \code{"nt:tfce_fwer"} (needs the optional \code{neurothresh} package): adds \code{q}
(TFCE FWER-corrected p), \code{sig_mask} (1/0 at \code{alpha}), and \code{tfce} (the
TFCE-enhanced statistic map).
\item \code{"nt:cluster_fdr_perm"} (needs \code{neurothresh}): adds \code{q} (cluster-FDR q
broadcast to voxels) and \code{sig_mask}.
}

Required input assays are auto-derived (and persisted) when absent: e.g.
\code{fdr:bh} on a z-only GDS adds a derived \code{p} alongside \code{q}.
}

\seealso{
\code{\link[=posthoc]{posthoc()}}, \code{\link[=list_posthoc]{list_posthoc()}}, \code{\link[=list_assays]{list_assays()}}
}
