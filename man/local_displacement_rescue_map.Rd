% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/local-displacement-rescue-map.R
\name{local_displacement_rescue_map}
\alias{local_displacement_rescue_map}
\title{Whole-search Local Displacement Rescue map}
\usage{
local_displacement_rescue_map(
  split_a,
  split_b,
  centers = NULL,
  contrast = NULL,
  patch_radius_mm = 12,
  shift_radius_mm = 4,
  folds = 5L,
  min_subjects = 12L,
  covariance_shrinkage = 0.25,
  iterations = 20L,
  ridge = 1e-04,
  tolerance = 1e-05,
  n_resamples = 0L,
  seed = 1L,
  alpha = 0.05,
  min_prevalence = 0.7,
  max_refits = Inf
)
}
\arguments{
\item{split_a, split_b}{Realised independent subject-level split maps with
matching voxel space, subjects, contrasts, \code{beta}, and genuine \code{var} or
\code{se} assays.}

\item{centers}{Optional full-volume voxel indices defining the search. \code{NULL}
searches every active center with a complete patch.}

\item{contrast}{Optional contrast name or index.}

\item{patch_radius_mm}{One or more positive physical patch radii.}

\item{shift_radius_mm}{One or more positive maximum translation radii. Every
searched configuration must satisfy \code{patch >= 2 * shift}.}

\item{folds}{Number of deterministic subject cross-fitting folds.}

\item{min_subjects}{Minimum number of paired subjects.}

\item{covariance_shrinkage}{Fixed shrinkage of split-difference local noise
correlation toward identity, between zero and one.}

\item{iterations, ridge, tolerance}{Exact-refit optimization controls.}

\item{n_resamples}{Number of whole-search calibration resamples. Zero returns
uncalibrated tangent diagnostics only.}

\item{seed}{Integer calibration seed. The caller's random state is restored.}

\item{alpha}{Familywise level used for candidate admission and \code{ldr_flag}.}

\item{min_prevalence}{Minimum exact-model activation prevalence required for
a rescue flag. This separates a broadly shared displaced feature from a
small active subgroup.}

\item{max_refits}{Maximum exact confirmations. \code{Inf} confirms every admitted
candidate; a finite cap is conservative and is recorded as truncation.}
}
\value{
A group-level \code{\link{gds}} containing the corrected LDR map and component
maps. Inferential values are \code{NA} outside analyzable centers; \code{ldr_flag} is
zero wherever the complete rescue conjunction is not established.
}
\description{
Screens every eligible center in a prespecified voxel search with a
cross-fitted spatial-derivative tangent model, corrects activation,
displacement, and ordinary fixed-coordinate evidence over the complete
center-by-scale family, and passes screen discoveries through the exact
discrete-shift LDR reference model.
}
\section{Scientific contract}{

Local noise correlation is estimated from standardized paired-split
differences in training subjects and used only for held-out generalized
least-squares scoring. Activation and ordinary-MNI maxima are calibrated by
rerunning the complete tangent search under paired whole-subject sign flips.
Displacement maxima use a subject-level Rademacher wild bootstrap of centered
held-out cross-split tangent products, with one multiplier shared across the
search for each subject. \code{ldr_fwer_p} is the maximum of corrected activation
and displacement p-values.

A nonzero \code{ldr_flag} additionally requires a nonsignificant corrected
ordinary-MNI test, positive MNI loss, and exact soft-shift improvement over
both the aligned-positive and aligned signed-amplitude competitors. Exact
refitting can remove a tangent-screen discovery but cannot create one.
}

\section{Experimental}{

This is a translation-only voxel-space procedure requiring independent
paired split maps. With \code{centers = NULL}, the multiplicity family includes
every active center having a complete patch at each valid requested scale.
Supplying \code{centers} produces search-set, not whole-brain, correction. With
\code{n_resamples = 0}, corrected p-values are missing, no exact refits are run,
and \code{ldr_flag} is zero. Use at least 999 resamples for confirmatory work.
The tangent screen uses split-difference spatial covariance; the exact
confirmation currently retains the v0.1 diagonal likelihood.
}
