% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/neuropublish.R
\name{as_neuropublish.gds}
\alias{as_neuropublish.gds}
\title{Convert a realised GDS group result to a Neuropublish result}
\usage{
as_neuropublish.gds(
  x,
  title = "fmrigds group result",
  synopsis = "A realised group-level fmrigds result.",
  assays = NULL,
  diagnostics = character(),
  measure_overrides = NULL,
  labels = NULL,
  display = list(),
  staging = NULL,
  space_name = NULL,
  analysis_label = NULL,
  ...
)
}
\arguments{
\item{x}{A realised group-level \code{\link{gds}} in voxel space.}

\item{title, synopsis}{Manifest title and synopsis.}

\item{assays}{Optional character vector of assays to publish. The default is
effect, standard error, one primary t/z statistic, and random-effects
heterogeneity when available.}

\item{diagnostics}{Additional assay names to include as diagnostic fields.}

\item{measure_overrides}{Optional named measure IDs for custom fields. These
remain producer-authored, untrusted extension measures.}

\item{labels}{Optional named human labels keyed by assay name.}

\item{display}{Optional named Neuropublish display recommendations keyed by
field ID or assay name. Recommendations are validated by Neuropublish and
no threshold is invented when they are absent.}

\item{staging}{Directory used by Neuropublish to stage NIfTI assets.}

\item{space_name}{Optional human name for the voxel coordinate space.}

\item{analysis_label}{Optional label for the emitted analysis.}

\item{...}{Reserved for forward-compatible adapter options.}
}
\value{
A \code{neuropublish_result} from the optional \code{neuropublish} package.
}
\description{
This producer-owned adapter publishes a deliberately small set of scalar
volume fields from one realised voxel-space group result. A portable
reduction receipt is required; subject-level data, local source paths,
design matrices, and custom weight values are never placed in the manifest.
}
\details{
The \code{neuropublish} package is optional and its S3 method is registered only
after that namespace loads, in either package load order.
}
\examples{
\dontrun{
library(neuropublish)
result <- as_neuropublish(compute(reduce(plan, method = "meta:re")))
}
}
