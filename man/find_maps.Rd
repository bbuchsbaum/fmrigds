% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nifti-utils.R
\name{find_maps}
\alias{find_maps}
\title{Find NIfTI map files with optional subject/contrast hooks}
\usage{
find_maps(
  root,
  pattern = "\\\\.nii(\\\\.gz)?$",
  recursive = TRUE,
  subject_fun = NULL,
  contrast_fun = NULL
)
}
\arguments{
\item{root}{Root directory to search.}

\item{pattern}{Regular expression passed to \code{\link[base:list.files]{base::list.files()}} to match
candidate NIfTI files (e.g. \code{"AUC\\\\\\\\.nii(\\\\\\\\.gz)?$"}).}

\item{recursive}{Logical; whether to search subdirectories (default \code{TRUE}).}

\item{subject_fun}{Optional function applied to the character vector of file
paths, expected to return a character vector of subject identifiers.}

\item{contrast_fun}{Optional function applied to the character vector of
file paths, expected to return a character vector of contrast labels.}
}
\value{
A data.frame with columns \code{file}, \code{subject}, and \code{contrast}.
}
\description{
Convenience helper to discover NIfTI map files on disk using a flexible
regular-expression pattern. This is intended for quick "scooping up" of
first-level maps spread across a directory tree (e.g. \verb{.*/maps/AUC.nii}).
}
\details{
The function does not impose any file naming convention. Instead, callers
can provide optional \code{subject_fun} and \code{contrast_fun} hooks to derive
subject and contrast labels from file paths.
}
