% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export-neuroim.R
\name{as_gds.NeuroVec}
\alias{as_gds.NeuroVec}
\alias{as_gds.DenseNeuroVec}
\title{Convert a NeuroVec to GDS}
\usage{
\method{as_gds}{NeuroVec}(
  x,
  assay_name = "beta",
  along = c("subject", "contrast"),
  subjects = NULL,
  contrasts = NULL,
  mask = NULL,
  ...
)

\method{as_gds}{DenseNeuroVec}(
  x,
  assay_name = "beta",
  along = c("subject", "contrast"),
  subjects = NULL,
  contrasts = NULL,
  mask = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{neuroim2::NeuroVec} object.}

\item{assay_name}{Name for the assay (default: \code{"beta"}).}

\item{along}{What does the 4th dimension represent? One of:
\itemize{
\item \code{"subject"}: Each volume is a different subject (single contrast)
\item \code{"contrast"}: Each volume is a different contrast (single subject)
}}

\item{subjects}{Character vector of subject IDs. Length must match the 4th
dimension when \code{along = "subject"}, or length 1 when \code{along = "contrast"}.}

\item{contrasts}{Character vector of contrast names. Length must match the 4th
dimension when \code{along = "contrast"}, or length 1 when \code{along = "subject"}.}

\item{mask}{Optional mask. See \code{as_gds.NeuroVol} for options.}

\item{...}{Additional arguments passed to \code{\link[=new_gds]{new_gds()}}.}
}
\value{
A \code{gds} object with voxel space.
}
\description{
Import a \code{neuroim2::NeuroVec} (4D brain volume) as a GDS object.
The 4th dimension can represent subjects OR contrasts.
}
\examples{
\dontrun{
# 4D volume where each volume is a subject
vec4d <- neuroim2::read_vec("all_subjects.nii.gz")
gds_obj <- as_gds(vec4d, along = "subject",
                  subjects = c("sub-01", "sub-02", "sub-03"))

# 4D volume where each volume is a contrast
gds_obj <- as_gds(vec4d, along = "contrast",
                  contrasts = c("faces", "places", "objects"))
}
}
