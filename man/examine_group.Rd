% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/examine-group.R
\name{examine_group}
\alias{examine_group}
\title{Examine subjects under an intended group model}
\usage{
examine_group(
  x,
  method = NULL,
  formula = NULL,
  options = list(),
  quality = NULL,
  estimands = NULL,
  retain = NULL,
  control = examination_control(),
  na_action = c("fail", "exclude")
)
}
\arguments{
\item{x}{A subject-level \code{\link{gds_plan}}, \code{\link{gds_source}}, or \code{\link{gds}}, optionally
containing one group reducer and a post-hoc conclusion tail.}

\item{method}{Reducer method when \code{x} has no reducer.}

\item{formula}{Model formula when \code{x} has no reducer.}

\item{options}{Reducer options when \code{x} has no reducer.}

\item{quality}{Optional character vector naming first-level quality columns
in \code{col_data(x)}. Metrics are descriptive unless an explicit direction and
absolute threshold are declared in \code{control$review$quality}.}

\item{estimands}{Named linear estimands or design-column names. See
\code{\link[=examination_control]{examination_control()}} for execution controls.}

\item{retain}{Optional subject IDs retained for later localization regardless
of review rank.}

\item{control}{A validated object from \code{\link[=examination_control]{examination_control()}}.}

\item{na_action}{Missing-covariate policy. The default fails; \code{"exclude"}
deterministically records and excludes affected subjects from this
examination only.}
}
\value{
A \code{gds_examination} object.
}
\description{
\code{examine_group()} branches from subject-level data at a group reducer. It
reports model-conditioned predictive surprise separately from each
subject's exact influence on requested group estimands. It never deletes a
subject or rewrites the source analysis.
}
\details{
When \code{x} already contains one reducer, the examination inherits that
reducer's method, formula, options, and weighting semantics. Supplying model
overrides in that case is an error. When no reducer is present, \code{method} is
required and formula-driven reducers default to \code{~ 1}.
}
\section{Result contract}{

The returned object keeps spatial maps in ordinary GDS objects and keeps
subject, contrast, estimand, availability, sensitivity, and provenance data
in tables or named lists. \code{subject_data$review_priority} orders inspection;
\code{subject_data$review_status} changes to \code{"review"} only when an absolute,
stability-aware criterion is met. Random-effects screening is labeled
\code{"tau2_fixed_full"}; exact retained-subject refits are labeled
\code{"tau2_refit_exact"} and never alter the original review queue.
}

\examples{
subject_ids <- paste0("sub-", 1:8)
participant_data <- data.frame(
  group = factor(rep(c("control", "patient"), each = 4)),
  row.names = subject_ids
)
beta <- array(rnorm(12 * 8, sd = 0.2), c(12, 8, 1))
beta[, participant_data$group == "patient", 1] <-
  beta[, participant_data$group == "patient", 1] + 0.4
variance <- array(0.1, dim(beta))
subject_gds <- new_gds(
  assays = list(beta = beta, var = variance),
  space = space_sample_labels(paste0("feature-", 1:12)),
  subjects = subject_ids,
  contrasts = "task",
  col_data = participant_data
)
analysis <- as_plan(subject_gds) |>
  reduce(method = "meta:re_reg", formula = ~ group)
exam <- examine_group(
  analysis,
  estimands = "grouppatient",
  control = examination_control(retain_n = 2L)
)
summary(exam)
if (requireNamespace("ggplot2", quietly = TRUE)) plot(exam)

}
