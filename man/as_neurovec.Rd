% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export-neuroim.R
\name{as_neurovec}
\alias{as_neurovec}
\title{Convert GDS assay to a 4D NeuroVec}
\usage{
as_neurovec(
  x,
  assay = "beta",
  along = c("subject", "contrast", "both"),
  subset_subjects = NULL,
  subset_contrasts = NULL
)
}
\arguments{
\item{x}{A GDS object with a voxel space.}

\item{assay}{Name of the assay to extract (default: \code{"beta"}).}

\item{along}{Which dimension to stack along the 4th dimension. One of:
\itemize{
\item \code{"subject"}: Stack subjects (collapses contrasts if multiple).
\item \code{"contrast"}: Stack contrasts (collapses subjects if multiple).
\item \code{"both"}: Stack all subject x contrast combinations.
}}

\item{subset_subjects}{Optional character vector of subject IDs to include.}

\item{subset_contrasts}{Optional character vector of contrast names to include}
}
\value{
A \code{neuroim2::NeuroVec} object with dimensions \verb{[x, y, z, n]} where
\code{n} depends on the \code{along} parameter.
}
\description{
Extract a single assay from a GDS object and return it as a
\code{neuroim2::NeuroVec} (4D neuroimaging array), stacking along the 4th
dimension.
}
\examples{
\dontrun{
# Stack all subjects into a 4D volume
vec4d <- as_neurovec(gds, assay = "beta", along = "subject")

# Stack specific contrasts
vec4d <- as_neurovec(gds, assay = "t", along = "contrast",
                     subset_contrasts = c("faces", "places"))
}
}
