% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog.R
\name{map_assays}
\alias{map_assays}
\title{Map files to assay types}
\usage{
map_assays(catalog, ...)
}
\arguments{
\item{catalog}{An image_catalog object.}

\item{...}{Named arguments where names are assay types and values are either:
\itemize{
\item A regex pattern to match against filenames, OR
\item A named list like \code{list(column = value)} for exact column matching
}}
}
\value{
Updated image_catalog with assay_map.
}
\description{
Specify which files correspond to which statistical assays (beta, se, var, t).
This mapping is used by \code{\link[=as_gds.image_catalog]{as_gds.image_catalog()}} to construct the GDS.
}
\examples{
\dontrun{
# By filename pattern
catalog <- map_assays(catalog,
  beta = "cope[0-9]+\\\\.nii",
  se = "varcope[0-9]+\\\\.nii"
)

# By metadata column value
catalog <- map_assays(catalog,
  beta = list(stat_type = "cope"),
  se = list(stat_type = "varcope")
)
}
}
