% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export-neuroim.R
\name{as_neurovol_list}
\alias{as_neurovol_list}
\title{Convert GDS assay to list of NeuroVol objects}
\usage{
as_neurovol_list(
  x,
  assay = "beta",
  by = c("subject", "contrast"),
  drop_dim = TRUE
)
}
\arguments{
\item{x}{A GDS object with a voxel space.}

\item{assay}{Name of the assay to extract (default: \code{"beta"}).}

\item{by}{How to organize the output list. One of:
\itemize{
\item \code{"subject"}: list indexed by subject, each element is a NeuroVol
(only works when there's a single contrast)
\item \code{"contrast"}: list indexed by contrast, each element is a NeuroVol
(only works when there's a single subject)
}}

\item{drop_dim}{If \code{TRUE} (default), drop singleton dimensions when
extracting. If \code{FALSE}, always return a nested list structure.}
}
\value{
A named list of \code{neuroim2::NeuroVol} objects. The structure depends
on the \code{by} parameter and the dimensions of the data:
\itemize{
\item Single contrast, \code{by = "subject"}: list of NeuroVol named by subject
\item Single subject, \code{by = "contrast"}: list of NeuroVol named by contrast
\item Multiple of both: nested list \verb{[[subject]][[contrast]]}
}
}
\description{
Extract a single assay from a GDS object and return it as a list of
\code{neuroim2::NeuroVol} objects, one per subject/contrast combination.
}
\examples{
\dontrun{
# Extract beta maps as list of NeuroVols (one per subject)
vols <- as_neurovol_list(gds, assay = "beta")
vols$sub01  # NeuroVol for subject "sub01"

# Extract by contrast
vols_by_con <- as_neurovol_list(gds, assay = "t", by = "contrast")
}
}
