% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog.R
\name{image_catalog}
\alias{image_catalog}
\title{Discover image files and create a catalog}
\usage{
image_catalog(root, pattern = "**/*.nii*", path_regex = NULL, recursive = TRUE)
}
\arguments{
\item{root}{Root directory to search.}

\item{pattern}{Glob pattern for file discovery (e.g., \code{"sub-*/analysis/*.nii.gz"}).
Supports \code{*} for single directory level and \verb{**} for recursive matching.}

\item{path_regex}{Optional regex with named capture groups for metadata extraction.
Named groups like \verb{(?<subject>[^/]+)} become metadata columns.}

\item{recursive}{Whether to search subdirectories. Default \code{TRUE}. Only used
if pattern does not contain \verb{**}.}
}
\value{
An image_catalog object.
}
\description{
Sweeps a directory tree for files matching a glob pattern and extracts
metadata from file paths using regex capture groups.
}
\examples{
\dontrun{
# Discover files with subject extraction
catalog <- image_catalog(
  root = "study",
  pattern = "sub-*/analysis/*.nii.gz",
  path_regex = "sub-(?<subject>[^/]+)"
)
}
}
